# 2026.260.1018 — feat: author the `charly candy` command skill

## Summary

`charly candy` — the candy-manifest authoring surface had no owning skill. Adds a `skill:` entity as a SIBLING top-level node
(`candy-skill:` beside the candy node) in `candy/<plugin>/charly.yml` — the `skill:`
kind's authored shape. It declares `family: candy` (declared in
opencharly/pod-charly-hooks#6, MERGED) and documents the command's surface.

The generated marketplace corpus is never hand-edited; this candy entity is its
source.

## How tested

**The entity's emitted artifact is produced — and disappears without it**
(discriminator, run with the MAINLINE generator `v0.2026241.1531`, the version
`tools/marketplace-cli/go.mod` pins; not a patched build):
```
$ /tmp/mp-mainline generate --root . --out ./out     # probe root: this candy + the marketplace families entity
marketplace generate: wrote 86 artifact(s) across 38 family(ies)
$ head -3 out/candy/skills/candy/SKILL.md
---
name: candy
description: |-

$ # delete the "candy-skill:" node, regenerate
$ /tmp/mp-mainline generate --root . --out ./outX
marketplace generate: wrote 85 artifact(s) across 38 family(ies)
$ ls outX/candy/skills/candy/SKILL.md
ls: cannot access 'outX/candy/skills/candy/SKILL.md': No such file or directory
```

**Manifest gate (pasted, with binary provenance):**
```
$ charly version
v2026.238.1242 (this repo's CI pin)
$ charly box validate
exit 0 (the v2026.238.1242 CI pin validates the manifest cleanly; the exit code is the verdict on its schema)
```


**In-tree coverage that FAILS without the change (the plan's own discriminator).**
The candy's plan now carries a deterministic build-context check; its command run
directly, before and after removing the entity:
```
$ grep -q '^candy-skill:' charly.yml && echo PRESENT
PRESENT                                   # exit 0
$ # remove the "candy-skill:" node
$ grep -q '^candy-skill:' charly.yml; echo exit=$?
exit=1
```
So `charly box validate` + the plan discriminate the entity's presence (the
candy's plan-step idiom), not just its schema shape.

**Family dependency is on the consumer's MERGED `main`** (pod-charly-hooks#6 merged 2026-09-17T09:19:11Z):
```
$ git -C pod-charly-hooks show origin/main:charly.yml | grep -n '^            candy:'
945:            candy:
```

**Documented surface provenance.** The shipped command's own surface:
```
$ sed -n '47,49p' candy/plugin-candy/command.go
var candyUsage = `usage: charly candy <set <name> <path> <value> | ` +
    strings.Join(addFormatWords(), "|") + ` <name> <pkg…>>`
(sectionDistroPath maps each add-<fmt> verb to its distro: section)
```

**Cross-references resolve (pasted):**
```
$ for ref in '/charly-image:layer' '/charly-image:image'; do
    fam=${ref#/charly-}; fam=${fam%%:*}; name=${ref##*:}
    [ -f "marketplace/$fam/skills/$name/SKILL.md" ] && echo "OK   $ref" || echo "MISS $ref"
  done
OK   /charly-image:layer
OK   /charly-image:image
```

## Rulebook compliance

| Rule | How this PR satisfies it |
|---|---|
| R0 — skills first | git-workflow, skills, marketplace, check loaded before acting. |
| R1 — RCA every anomaly | Gate zero warnings/errors; the family dependency read from the merged consumer. |
| R2 — finish the whole cutover | Skill + family (#6, MERGED) + the marketplace refs-list bump/regeneration land as one coupled set; the regeneration is the named exit (`opencharly/marketplace`, the refs-list + corpus PR) — this repo's entry in it is the projection. |
| R3 — no duplication | One `skill:` entity; documents the existing command. |
| R4 — no workarounds | Every command the skill names is the actual `charly` verb. |
| R4a — product-first docs | The documented surface is the shipped command (pasted above). |
| R5 — delete legacy | Additive only; a previously-dangling sibling ref was removed. |
| R6 — git safety | Branch-per-change PR; no force-push; clean tree. |
| R7 — prove behaviour | The mainline-generator discriminator above: present→emitted, removed→gone. |
| R8 — preserve emitted artifacts | The emitted SKILL.md path is asserted by the generator run (pasted). |
| R9 — binary equals source | Generator = pinned mainline version (pasted); validate binary provenance stated. |
| R10 — fresh disposable proof | The discriminator above IS the class gate — a `skill:` entity's only product is its corpus projection; prose cannot fail a runtime bed. |
| RDD/ADE/SDD | ADE: a `skill:` entity is not a candy, so no candy `description:`/`plan:` is required. SDD: the entity validates against the served `#SkillInput` schema. |
| Disposable-only gate | N/A — nothing created/destroyed. |
| Hooks doctrine | N/A — standalone repo; merge gate = org pr-validator. |

## Change classification

- **Class:** candy manifest entity (config) — an additive authored `skill:` entity.
- **Verification gate:** the mainline-generator discriminator (pasted) +
  `charly box validate` (pasted) + the named marketplace regeneration projection.
- **Attribution tier:** `analysed on a live system` — the generator and validate
  paths ran live with retained output; the corpus projection is the named
  regeneration gate.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*